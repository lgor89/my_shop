<%= form_for(@product) do |f| %>
    <%= f.text_area :name %></br>
    <%= f.text_area :price %></br>
    <%= f.submit "Create product", class: "btn btn-primary" %></br>
    <span class="picture">
    <%= f.file_field :picture %>
  </span>
<% end %>